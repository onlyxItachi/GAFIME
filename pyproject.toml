[project]
name = "gafime"
version = "0.0.0"
description = "Internal GPU-accelerated feature interaction mining engine"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
]

[tool.pytest.ini_options]
addopts = "-q"
