# GAFIME v0.4.6 Metal Native Backend Notes

This document tracks the Metal backend implementation and validation evidence
for v0.4.6. It is intentionally focused on architecture and runtime behavior.

## Scope

- Native Apple Silicon Metal backend through `NativeMetalBackend`.
- Shared-memory Metal matrix ABI for continuous interaction batches.
- Continuous interaction arity support from 1 through 5.
- Soft discrete feature scoring on Metal.
- Time-series feature scoring on Metal for lag, delta, velocity,
  acceleration, rolling mean, rolling standard deviation, and rolling sum.
- Adaptive soft-discrete selector metrics on Metal:
  - `mutual_info`
  - `variance_reduction`
  - `residual_abs_corr`
  - `residual_r2_gain`
- Native structured `DiagnosticReport` access. `to_dict()` is a deprecated
  export boundary, not a framework data-flow path.
- GitHub Apple Silicon workflows for installed-wheel smoke, stress, and
  benchmark validation.

## Backend Behavior

Metal now uses package-local `gafime_metal.dylib` plus
`gafime_kernels.metallib` when available. The Python wrapper sets the metallib
path before probing the device so installed wheels can load shaders without
depending on the process working directory.

`backend="auto"` uses platform-aware priority. On macOS it tries Metal first
and then C++ Core. Explicit `backend="cuda"` on macOS must fail with a clear
unsupported-platform error. Explicit `backend="metal"` on non-macOS platforms
must fail similarly.

The main continuous path uses a global matrix buffer with
`MTLResourceStorageModeShared`, which matches Apple Silicon unified memory. The
host still copies Python/native buffers into shared `MTLBuffer` storage, but it
does not perform PCIe-style host/device transfers.

Hard discrete mode remains unsupported on GPU backends and must raise:

```text
GPU feature engineering with discrete hard mode is not supported!
```

## Validation Status

- Metal report metrics are computed natively for `pearson` and `r2`.
  `spearman` and `mutual_info` report completion currently routes through the
  C++ Core backend.
- Metal shader compilation and runtime behavior were validated on GitHub
  Actions run `27013277804`, workflow `Metal Beast Benchmark #2`, job
  `Metal beast on macos-26`.
- The successful run used `tests/metal_hardcore_benchmark.py` against an
  installed macOS ARM64 wheel on a `macos-26` Apple M1 Virtual runner.
- The workflow artifact `metal-beast-macos-26` captured:
  `runner-specs.json`, `metal-device.json`, `metal-hardcore-macos-26.json`,
  and `summary.md`.
- The artifact was collected before the final v0.4.6 metadata bump, so its
  wheel filename reports `0.4.5`; the validated Metal code is the v0.4.6
  release candidate code.

Representative result from that run:

```text
platform: macOS 26.4 arm64, Apple M1 (Virtual), 3 cores, 7 GB RAM
Metal device: Apple Paravirtual device Apple0, unified memory
direct continuous scan: 55,454 candidates in 1.283 s
direct continuous throughput: 1.416B candidate rows/s
direct discrete scan: 1,710 candidates in 0.0597 s
full Engine run: 19,886 interactions in 8.365 s
families: interaction=12,950, discrete_function=6,000, time_series_function=936
```

## Validation

Manual workflow:

```bash
gh workflow run metal_macos26_lab.yml
```

To force the larger runner after it is enabled:

```bash
gh workflow run metal_macos26_lab.yml -f runner_label=macos-26-xlarge
```

Expected artifacts:

- `runner-specs.txt`
- `metal-device.txt`
- `wheel-contents.txt`
- `installed-wheel-metal-smoke.json`
- one macOS arm64 lab wheel

Linux-side checks before dispatching the Metal lab:

```bash
python -m py_compile gafime/backends/native_metal_backend.py gafime/reporting/report.py gafime/cli.py
python tests/test_v045_native_spine.py
python -m gafime.cli --check
```
