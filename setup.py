from __future__ import annotations

raise SystemExit(
    "GAFIME v1 is built with maturin from crates/gafime-py. "
    "Use `python -m build` or `maturin build`; setup.py no longer builds runtime artifacts."
)
