name: Build and Publish Wheels

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]
        # ubuntu-latest = Linux x86_64
        # windows-latest = Windows x86_64
        # macos-14 = macOS arm64 (Apple Silicon)

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Windows specific: Setup MSVC
      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      # Windows specific: Setup CUDA Toolkit so we can build gafime_cuda.dll natively
      - name: Setup CUDA Toolkit (Windows)
        if: runner.os == 'Windows'
        uses: Jimver/cuda-toolkit@master
        id: cuda-toolkit
        with:
          cuda: '13.1.0'

      # Linux specific: CUDA requires installing toolkit inside the manylinux container
      # We let cibuildwheel handle Linux containers, but we need to configure it

      - name: Install cibuildwheel
        run: python -m pip install "cibuildwheel>=2.22.0"

      - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          # Limit Python versions to save time in CI (3.10 to 3.14)
          CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-* cp314-*"
          CIBW_SKIP: "pp* *-musllinux_*" # Skip PyPy and musl
          CIBW_ARCHS_MACOS: "arm64"
          CIBW_ARCHS_LINUX: "x86_64"
          CIBW_ARCHS_WINDOWS: "AMD64"
          
          # Upgrade from CentOS 7 (manylinux2014) to AlmaLinux 8 (manylinux_2_28)
          CIBW_MANYLINUX_X86_64_IMAGE: "manylinux_2_28"
          
          # We need Rust to compile gafime_cpu, and the CUDA Toolkit to compile gafime_cuda
          CIBW_ENVIRONMENT: "PATH=$HOME/.cargo/bin:$PATH"
          CIBW_BEFORE_ALL_LINUX: "curl https://sh.rustup.rs -sSf | sh -s -- -y && dnf config-manager --add-repo https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-rhel8.repo && dnf clean all && dnf install -y cuda-nvcc-13-1 cuda-cudart-devel-13-1"
          CIBW_BEFORE_ALL_MACOS: "curl https://sh.rustup.rs -sSf | sh -s -- -y && source $HOME/.cargo/env"
          CIBW_BEFORE_ALL_WINDOWS: "rustup default stable"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build sdist
        run: pipx run build --sdist

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cibw-sdist
          path: dist/*.tar.gz

  release:
    name: Publish to GitHub Releases
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: cibw-*
          path: dist
          merge-multiple: true

      - name: Release Note
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
